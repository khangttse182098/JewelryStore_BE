package com.swp.jewelrystore.api;

import org.springframework.stereotype.Controller;

@Controller
public class WebSocketController {
    // hàm trả ra dữ liệu dạng JSON cho front-end
    // trả ra list danh sách bên sell-order
    // mapped as app/application (có thể đổi được đường dẫn)
    // @MessageMapping("/application")
    // @SendTo(đường dẫn để front-end lấy dữ liệu)
}
